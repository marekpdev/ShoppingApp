<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data />

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/ivShipping"
            app:layout_constraintTop_toTopOf="@id/tvDeliveryAddressLabel"
            app:layout_constraintBottom_toBottomOf="@id/tvDeliveryAddressLabel"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_margin="16dp"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/outline_local_shipping_24"/>

        <TextView
            android:id="@+id/tvDeliveryAddressLabel"
            android:layout_width="0dp"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginStart="16dp"
            android:text="Delivery Address"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/ivShipping"
            app:layout_constraintEnd_toStartOf="@id/ivEdit"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvDeliveryAddress"
            android:layout_width="0dp"
            android:textSize="18sp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="Shipping"
            app:layout_constraintStart_toStartOf="@id/tvDeliveryAddressLabel"
            app:layout_constraintEnd_toStartOf="@id/ivEdit"
            app:layout_constraintTop_toBottomOf="@id/tvDeliveryAddressLabel" />

        <ImageView
            android:id="@+id/ivEdit"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="16dp"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/outline_edit_24"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>